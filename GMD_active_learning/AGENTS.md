# AGENTS.md

1. 不要破坏模块边界。
2. 不要把主动学习逻辑写进 `GMD` 或 `GMD_se3gnn`。
3. 所有新增代码必须有测试。
4. 所有外部项目调用必须通过 adapter。
5. 不允许硬编码绝对路径。
6. 所有阈值必须来自 YAML 配置。
7. 不要引入不必要的大型依赖。
8. `PyTorch` 只用于 `reliability_model` 与必要张量计算，不要强行重写所有 `numpy` 几何逻辑。
9. 所有模块应具备类型注解、日志与基础异常处理。
10. 所有功能都应支持通过配置文件启用或关闭。
